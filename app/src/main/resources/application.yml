spring:
  batch:
    job:
      enabled: false
  security:
    enabled: false
#  jpa:
#    properties:
#      hibernate:
#        default_batch_fetch_size: 1000

jwt:
  validity-time: 10m # minute
  issuer: Hotel-java
  secret: Object-oriented programming is a programming paradigm based on the concept of objects, which can contain data and code

hotel-java.iamport:
  apiKey: ${IAMPORT_KEY}
  apiSecret: ${IAMPORT_SECRET}

hotel-java.batch:
  stock:
    chunkSize: 10
    day: 30
    quantity: 10
